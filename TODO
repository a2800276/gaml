> includes
better tests
!!! doctypes (and maybe corresponding options)

rendering option:
  indent
  quoting, escaping

samples for combining with go templates

http handler

list (configurable) of autoclosing tags (<br/>)
