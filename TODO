better tests
rendering option:
  indent
  quoting, escaping


http handler

list (configurable) of autoclosing tags (<br/>)

interface work, much better template integration
  ignore {{}} go templates in tags and attributes.

> includes (can be handled through go templates ...)
